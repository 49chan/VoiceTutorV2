# 2026-07-30 작업 정리 및 인수인계 문서 (인수인계_20270731)

본 문서는 오늘 진행된 모든 설정 보완 및 버그 수정 사항을 정리한 인수인계 파일입니다. 다음 세션 진입 시 토큰 절약을 위해 새로운 대화창을 시작하고 이 파일을 먼저 AI에게 로드해 주세요.

---

## 🛠️ 금일 작업 완료 및 반영 사항

### 1. 첫화면(Landing) 테스트 버전 안내 문구 통합
* **파일**: [frontend/index.html](file:///d:/12. Antigravity/20260724_KCoach_v2/frontend/index.html)
* **수정 내용**: 기존의 PC/모바일 구분 리스트 형식을 아래의 직관적인 문구로 통합 수정했습니다:
  > *"사용 시점의 평가결과와 결과이력만 화면에 표시되며, 생성된 편집(.txt), 녹음(.mp3), 평가(.json) 파일이 로컬에 저장되지 않습니다. (로컬 저장은 추후 지원 예정)"*

### 2. 설정 화면 백엔드 URL 옵션 변경 및 클라우드 저장 실패 원인 해결
* **파일**: [index.html](file:///d:/12. Antigravity/20260724_KCoach_v2/frontend/index.html), [app.js](file:///d:/12. Antigravity/20260724_KCoach_v2/frontend/js/app.js), [auth/callback/index.html](file:///d:/12. Antigravity/20260724_KCoach_v2/frontend/auth/callback/index.html)
* **수정 내용**:
  * **기본값 (클라우드)** 라디오 버튼을 왼쪽(첫 번째), **로컬테스트** 라디오 버튼을 오른쪽(두 번째)으로 스왑 배치하고 클라우드를 디폴트(`checked`)로 설정했습니다.
  * 백엔드 API 서버 URL의 기본값을 기존 `http://127.0.0.1:8000`에서 접속 환경 도메인(`window.location.origin`)으로 동적 변경하여 캐시가 없는 상태에서도 클라우드가 즉시 동작하도록 보완했습니다.
  * **저장 실패(404 NOT_FOUND) 해결**: 기존 Vercel 배포의 Root Directory가 `frontend`로 지정되어 있어 루트의 `api` 폴더가 누락되었던 사실을 파악하고, 최상위 루트 경로에서 Vercel 배포를 동기화하여 실서버 백엔드(`/api/settings` 및 `/api/evaluate`)를 정상 가동시켰습니다.

### 3. Azure 서버 8초 타임아웃 예외 강제 차단 및 한글 팝업 보완
* **파일**: [app.js](file:///d:/12. Antigravity/20260724_KCoach_v2/frontend/js/app.js) (`submitAssessment()`)
* **수정 내용**: 일부 브라우저에서 `AbortController` 취소 시 `AbortError` 대신 일반 통신 에러(`TypeError`)로 예외 형식을 우회하여 경고창이 작동하지 않던 버그를 잡기 위해 `isTimeout` 불리언 검증 플래그를 추가했습니다. 8초 초과 시 예외 형식에 상관없이 강제 중단 팝업이 무조건 뜹니다.

### 4. 영어(`en-US`) 평가 결과 한글 번역 처리
* **파일**: [api/index.py](file:///d:/12. Antigravity/20260724_KCoach_v2/api/index.py) (`generate_feedback()`)
* **수정 내용**: 학습 언어가 영어인 경우 영문 코멘트 대신 한글 번역 코멘트(예: "훌륭한 발음입니다! 단어 강세, 억양 및 흐름이 매우 자연스럽습니다.")를 매칭해 리턴하도록 수정했습니다. 이를 통해 브라우저 화면 표시와 로컬 저장되는 `.json` 파일의 `summary_feedback` 텍스트가 한글화됩니다.

### 5. 설정 화면 저장 경로 삭제 및 기본 경로 강제화
* **파일**: [index.html](file:///d:/12. Antigravity/20260724_KCoach_v2/frontend/index.html), [app.js](file:///d:/12. Antigravity/20260724_KCoach_v2/frontend/js/app.js), [config.py](file:///d:/12. Antigravity/20260724_KCoach_v2/api/config.py), [test_backend_pipeline.py](file:///d:/12. Antigravity/20260724_KCoach_v2/api/test_backend_pipeline.py)
* **수정 내용**:
  * 설정 드로어 창에서 수동으로 로컬 파일 저장 위치를 적는 인풋 필드를 제거했습니다.
  * 백엔드 설정 객체 로딩 및 저장 시, 로컬 테스트 환경인 경우 `settings.json` 저장 경로를 항상 시스템 기본 경로(`C:\Users\<사용자명>\VoiceTutor_Records`)로 강제하도록 백엔드 함수를 고정했습니다.
  * 관련 변경에 맞춰 백엔드 단위 테스트 검증 코드의 어설션도 보완하여 모든 테스트 정상 통과를 확인했습니다.

### 6. 평가 시 설정화면 학습언어 연동 처리
* **파일**: [app.js](file:///d:/12. Antigravity/20260724_KCoach_v2/frontend/js/app.js), [index.py](file:///d:/12. Antigravity/20260724_KCoach_v2/api/index.py)
* **수정 내용**: 평가 단추 클릭 시점의 설정화면 학습 언어 선택 값(`#setting-learning-lang`)을 가로채서 API `/api/evaluate` 요청 폼 데이터(`learning_language`)에 실어 보냅니다. 백엔드는 이 클라이언트 측 전달 언어를 최우선으로 사용하여 발음 평가 프로세스를 분기합니다.

---

## 🚀 현물 상태 및 검증 대기
* 모든 소스코드는 로컬 단위 테스트 패스를 마쳤으며, 깃허브 원격 저장소(`main` 브랜치)에 안전하게 PUSH 되었습니다.
* Vercel 프로덕션 도메인(`https://voicetutor-sand.vercel.app`)에도 모든 사항이 정상적으로 반영(빌드 및 배포 완료)되었습니다.
